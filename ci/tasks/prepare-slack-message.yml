---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: python

inputs:
- name: ci-branch
- name: slack-message-input

outputs:
- name: slack-message-output

run:
  path: ci-branch/ci/scripts/prepare-slack-message.py

params:
  MESSAGE: 'Test'
  BUTTONS:
    - text: Test
      url: slack-message-input/url.txt
      type: button
      style: primary
